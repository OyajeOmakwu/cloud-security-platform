apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-namespace-traffic
  namespace: default
spec:
  rules:
    - from:
        - source:
            namespaces: ["default"]
