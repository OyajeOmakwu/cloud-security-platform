apiVersion: v1
kind: Pod
metadata:
  name: secure-pod
spec:
  containers:
    - name: app
      image: nginx:stable
      securityContext:
        privileged: false
      resources:
        limits:
          cpu: "500m"
          memory: "256Mi"